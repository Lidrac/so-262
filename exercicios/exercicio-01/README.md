Execute o seguinte prompt na IA Perplexity.

Crie uma aplicação Rust que implementa um processo com duas threads (produtor e consumidor). As duas threads acessam um buffer limitado. Deve haver exclusão mútua para acessar o buffer que é compartilhado entre as duas threads.
